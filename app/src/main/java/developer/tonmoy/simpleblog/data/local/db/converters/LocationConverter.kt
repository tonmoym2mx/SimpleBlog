package developer.tonmoy.simpleblog.data.local.db.converters

import androidx.room.TypeConverter


/*
class LocationConverter {
    @TypeConverter
    fun jsonStringToObject(jsonString:String): List<ProjectLocation> {
        return Gson().fromJson(jsonString,Array<ProjectLocation>::class.java).toList()
    }

    @TypeConverter
    fun objectToJsonString(objects: List<ProjectLocation>?): String? {
        return  Gson().toJson(objects)
    }
}*/
